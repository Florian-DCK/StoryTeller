<div class="flex flex-col items-center px-4 py-2 mb-4 border relative">
    <div class="flex flex-col mb-2 w-full">
        <h3 class="font-family-joan font-bold uppercase">{{title}}</h3>
        <div class="bg-black h-0.5 w-1/5"></div>
    </div>
    <div class="flex flex-col items-center mb-2 font-family-joan">
        <p>{{content}}</p>
    </div>    
    <p class="text-faded-text absolute top-0 right-2">{{created_at}}</p>
</div>